<div class="no-access-container">
    <div class="no-access-content animate-on-scroll">
        <div class="no-access-icon">
            <i class="fas fa-lock"></i>
        </div>
        <h1 class="no-access-title">Access Denied</h1>
        <p class="no-access-message">You don't have permission to access the staff portal. This area is restricted to Asteroid Studios staff members only.</p>
        <div class="no-access-actions">
            <a href="/" class="btn-primary">
                <i class="fas fa-arrow-left"></i> Return to Home
            </a>
        </div>
    </div>
</div>

<style>
    .no-access-container {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 70vh;
    }
    
    .no-access-content {
        text-align: center;
        max-width: 600px;
        padding: var(--space-xl);
    }
    
    .no-access-icon {
        font-size: 5rem;
        color: var(--danger);
        margin-bottom: var(--space-lg);
    }
    
    .no-access-title {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: var(--space-md);
        color: var(--light);
    }
    
    .no-access-message {
        font-size: 1.2rem;
        color: var(--gray);
        margin-bottom: var(--space-xl);
    }
    
    .no-access-actions {
        margin-top: var(--space-xl);
    }
</style>